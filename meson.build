project(
    'project',
    'cpp',
    version : '1.0.0',
    meson_version: '>=0.61.2',
    default_options: ['warning_level=3', 'werror=true', 'cpp_std=gnu++17', 'optimization=g'],
)

subdir('src')

# options
# unit_test = get_option('unit_test')
# if unit_test
#     subdir('tests')
# endif
