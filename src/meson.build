src_files = []
inc_dir = []

inc_dir += include_directories('.')

src_files += files('main.cpp')

# subdir('util')

executable('main', sources: src_files, include_directories: inc_dir, dependencies: [])
